<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.ShopPortal22BoleTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.AppCompat.ShopPortal22BoleTheme" parent="Theme.AppCompat.Light">
        <item name="android:colorForeground">@color/foreground_material_light_1</item>
        <item name="android:textColorPrimary">@color/text_color_primary</item>
    </style>

    <style name="JobsPortal22BoleTheme" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@android:color/holo_blue_dark</item>
        <item name="android:textColorPrimary">@android:color/holo_blue_dark</item>
    </style>

    <style name="BusinessPortal22BoleTheme" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@android:color/holo_orange_dark</item>
        <item name="android:textColorPrimary">@android:color/holo_orange_dark</item>
    </style>

</resources>
